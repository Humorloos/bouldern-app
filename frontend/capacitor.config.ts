// @ts-ignore
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'humorloos.pythonanywhere.com',
  appName: 'Bouldern App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
